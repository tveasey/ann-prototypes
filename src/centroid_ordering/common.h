#pragma once

#include <cstddef>
#include <utility>
#include <vector>

using Points = std::vector<float>;
using Permutation = std::vector<std::size_t>;
using PermutationCost = std::pair<Permutation, float>;
